load("@angular//:index.bzl", "ng_module")

package(default_visibility = ["//visibility:public"])

ng_module(
  name = "front",
  srcs = glob(["*.ts"]),
  deps = glob([".env*"]),
  tsconfig = "//:tsconfig.json"
)
